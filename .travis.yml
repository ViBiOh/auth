language: go
sudo: required
services:
- docker
go: 1.9
after_success: |
  if [ "${TRAVIS_BRANCH}" == "master" ]; then
    bash <(curl -s https://codecov.io/bash)
    make docker-push
  fi
notifications:
  email: false
